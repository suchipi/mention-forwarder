{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "$schema": {
      "description": "Path or URL to this JSON Schema, which is how an editor knows to offer completion and hover text for this file. The forwarder itself ignores it.",
      "type": "string",
      "markdownDescription": "Path or URL to this JSON Schema, which is how an editor knows to offer completion and hover text for this file. The forwarder itself ignores it."
    },
    "command": {
      "minItems": 1,
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Argv array to run for each mention. `command[0]` is the program and must be on `PATH` or an absolute path; the rest are its arguments.\n\nEvery entry may use any of these placeholders, and a misspelled one is reported at startup rather than passed through as literal text. Each also reaches the command as a `MENTION_*` environment variable, so `{{replyFile}}` is `MENTION_REPLY_FILE`.\n\n- `{{id}}` unique per delivery, so it works as a log or work-directory name. Example: `d3f1b2a0-8c4e-11f0-9a1b-3f2c6d5e7a8b`\n- `{{platform}}` which platform it came from: `github`, `slack`, or `linear`.\n- `{{kind}}` the event it arrived as. Examples: `issue_comment`, `pull_request_review_comment`, `commit_comment`, `discussion_comment`, `app_mention`, `message.im`, `comment`\n- `{{url}}` permalink to the comment or message that did the mentioning. Example: `https://github.com/octocat/hello-world/issues/7#issuecomment-3184920117`\n- `{{text}}` the comment or message body, verbatim. Example: `@my-bot please retry the flaky test`\n- `{{prompt}}` the same text with the mention itself removed, which is usually what you feed an agent. Example: `please retry the flaky test`\n- `{{author}}` who wrote it: a GitHub login, a Slack display name, or a Linear user name. Example: `octocat`\n- `{{title}}` the issue or PR title, or the channel id on Slack. Empty when the platform offers none. Example: `Flaky test on main`\n- `{{conversationKey}}` the thread whose mentions are forwarded one at a time. Examples: `github:octocat/hello-world#7`, `slack:T024BE7LD:C0G9QF9GW:1755973451.000100`, `linear:9f2c1e40-6b7a-4d02-9a11-5c8e2f0b3d64`\n- `{{receivedAt}}` when the forwarder accepted it, ISO 8601. Example: `2026-08-23T18:04:11.007Z`\n- `{{replyFile}}` path to append a reply to; write nothing there and nothing is posted. Example: `/tmp/mention-forwarder-Ab3xY9/d3f1b2a0-8c4e-11f0-9a1b-3f2c6d5e7a8b.md`\n- `{{json}}` the entire mention as one JSON string, the same object the command is handed on stdin.",
      "markdownDescription": "Argv array to run for each mention. `command[0]` is the program and must be on `PATH` or an absolute path; the rest are its arguments.\n\nEvery entry may use any of these placeholders, and a misspelled one is reported at startup rather than passed through as literal text. Each also reaches the command as a `MENTION_*` environment variable, so `{{replyFile}}` is `MENTION_REPLY_FILE`.\n\n- `{{id}}` unique per delivery, so it works as a log or work-directory name. Example: `d3f1b2a0-8c4e-11f0-9a1b-3f2c6d5e7a8b`\n- `{{platform}}` which platform it came from: `github`, `slack`, or `linear`.\n- `{{kind}}` the event it arrived as. Examples: `issue_comment`, `pull_request_review_comment`, `commit_comment`, `discussion_comment`, `app_mention`, `message.im`, `comment`\n- `{{url}}` permalink to the comment or message that did the mentioning. Example: `https://github.com/octocat/hello-world/issues/7#issuecomment-3184920117`\n- `{{text}}` the comment or message body, verbatim. Example: `@my-bot please retry the flaky test`\n- `{{prompt}}` the same text with the mention itself removed, which is usually what you feed an agent. Example: `please retry the flaky test`\n- `{{author}}` who wrote it: a GitHub login, a Slack display name, or a Linear user name. Example: `octocat`\n- `{{title}}` the issue or PR title, or the channel id on Slack. Empty when the platform offers none. Example: `Flaky test on main`\n- `{{conversationKey}}` the thread whose mentions are forwarded one at a time. Examples: `github:octocat/hello-world#7`, `slack:T024BE7LD:C0G9QF9GW:1755973451.000100`, `linear:9f2c1e40-6b7a-4d02-9a11-5c8e2f0b3d64`\n- `{{receivedAt}}` when the forwarder accepted it, ISO 8601. Example: `2026-08-23T18:04:11.007Z`\n- `{{replyFile}}` path to append a reply to; write nothing there and nothing is posted. Example: `/tmp/mention-forwarder-Ab3xY9/d3f1b2a0-8c4e-11f0-9a1b-3f2c6d5e7a8b.md`\n- `{{json}}` the entire mention as one JSON string, the same object the command is handed on stdin."
    },
    "cwd": {
      "description": "Working directory for the command. Default: the directory the forwarder was started in.",
      "type": "string",
      "markdownDescription": "Working directory for the command. Default: the directory the forwarder was started in."
    },
    "env": {
      "default": {},
      "description": "Extra environment variables for the command, on top of the `MENTION_*` ones it always receives.\n\nValues may use any of these placeholders, and a misspelled one is reported at startup rather than passed through as literal text.\n\n- `{{id}}` unique per delivery, so it works as a log or work-directory name. Example: `d3f1b2a0-8c4e-11f0-9a1b-3f2c6d5e7a8b`\n- `{{platform}}` which platform it came from: `github`, `slack`, or `linear`.\n- `{{kind}}` the event it arrived as. Examples: `issue_comment`, `pull_request_review_comment`, `commit_comment`, `discussion_comment`, `app_mention`, `message.im`, `comment`\n- `{{url}}` permalink to the comment or message that did the mentioning. Example: `https://github.com/octocat/hello-world/issues/7#issuecomment-3184920117`\n- `{{text}}` the comment or message body, verbatim. Example: `@my-bot please retry the flaky test`\n- `{{prompt}}` the same text with the mention itself removed, which is usually what you feed an agent. Example: `please retry the flaky test`\n- `{{author}}` who wrote it: a GitHub login, a Slack display name, or a Linear user name. Example: `octocat`\n- `{{title}}` the issue or PR title, or the channel id on Slack. Empty when the platform offers none. Example: `Flaky test on main`\n- `{{conversationKey}}` the thread whose mentions are forwarded one at a time. Examples: `github:octocat/hello-world#7`, `slack:T024BE7LD:C0G9QF9GW:1755973451.000100`, `linear:9f2c1e40-6b7a-4d02-9a11-5c8e2f0b3d64`\n- `{{receivedAt}}` when the forwarder accepted it, ISO 8601. Example: `2026-08-23T18:04:11.007Z`\n- `{{replyFile}}` path to append a reply to; write nothing there and nothing is posted. Example: `/tmp/mention-forwarder-Ab3xY9/d3f1b2a0-8c4e-11f0-9a1b-3f2c6d5e7a8b.md`\n- `{{json}}` the entire mention as one JSON string, the same object the command is handed on stdin.",
      "type": "object",
      "propertyNames": {
        "type": "string"
      },
      "additionalProperties": {
        "type": "string"
      },
      "markdownDescription": "Extra environment variables for the command, on top of the `MENTION_*` ones it always receives.\n\nValues may use any of these placeholders, and a misspelled one is reported at startup rather than passed through as literal text.\n\n- `{{id}}` unique per delivery, so it works as a log or work-directory name. Example: `d3f1b2a0-8c4e-11f0-9a1b-3f2c6d5e7a8b`\n- `{{platform}}` which platform it came from: `github`, `slack`, or `linear`.\n- `{{kind}}` the event it arrived as. Examples: `issue_comment`, `pull_request_review_comment`, `commit_comment`, `discussion_comment`, `app_mention`, `message.im`, `comment`\n- `{{url}}` permalink to the comment or message that did the mentioning. Example: `https://github.com/octocat/hello-world/issues/7#issuecomment-3184920117`\n- `{{text}}` the comment or message body, verbatim. Example: `@my-bot please retry the flaky test`\n- `{{prompt}}` the same text with the mention itself removed, which is usually what you feed an agent. Example: `please retry the flaky test`\n- `{{author}}` who wrote it: a GitHub login, a Slack display name, or a Linear user name. Example: `octocat`\n- `{{title}}` the issue or PR title, or the channel id on Slack. Empty when the platform offers none. Example: `Flaky test on main`\n- `{{conversationKey}}` the thread whose mentions are forwarded one at a time. Examples: `github:octocat/hello-world#7`, `slack:T024BE7LD:C0G9QF9GW:1755973451.000100`, `linear:9f2c1e40-6b7a-4d02-9a11-5c8e2f0b3d64`\n- `{{receivedAt}}` when the forwarder accepted it, ISO 8601. Example: `2026-08-23T18:04:11.007Z`\n- `{{replyFile}}` path to append a reply to; write nothing there and nothing is posted. Example: `/tmp/mention-forwarder-Ab3xY9/d3f1b2a0-8c4e-11f0-9a1b-3f2c6d5e7a8b.md`\n- `{{json}}` the entire mention as one JSON string, the same object the command is handed on stdin."
    },
    "port": {
      "default": 3000,
      "description": "The single port all three platforms post to. Any port from `1` to `65535`. Default: `3000`.",
      "type": "integer",
      "minimum": 1,
      "maximum": 65535,
      "markdownDescription": "The single port all three platforms post to. Any port from `1` to `65535`. Default: `3000`."
    },
    "maxPayloadBytes": {
      "default": 5242880,
      "description": "Largest webhook body to accept, in bytes. A larger body is refused with `413`, and one sent without a `Content-Length` with `411`, both before anything reads it. Must be at least `1024`. Default: `5242880` (5 MiB).",
      "type": "integer",
      "minimum": 1024,
      "maximum": 9007199254740991,
      "markdownDescription": "Largest webhook body to accept, in bytes. A larger body is refused with `413`, and one sent without a `Content-Length` with `411`, both before anything reads it. Must be at least `1024`. Default: `5242880` (5 MiB)."
    },
    "trustedProxies": {
      "default": [
        "loopback",
        "uniquelocal"
      ],
      "description": "Which hops may be believed when they set `X-Forwarded-For`, which is where the source check reads the real client address from behind a tunnel or router. Each entry is an address, a CIDR range, or one of the names `loopback`, `linklocal`, and `uniquelocal`. Default: `[\"loopback\", \"uniquelocal\"]`.",
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1
      },
      "markdownDescription": "Which hops may be believed when they set `X-Forwarded-For`, which is where the source check reads the real client address from behind a tunnel or router. Each entry is an address, a CIDR range, or one of the names `loopback`, `linklocal`, and `uniquelocal`. Default: `[\"loopback\", \"uniquelocal\"]`."
    },
    "timeoutMs": {
      "default": 0,
      "description": "Kill the command after this many milliseconds. `0` means never, which is the right choice for a long-running agent. What it measures follows `lifecycle`: under the default `per-mention` it is one mention's run, and when `lifecycle` is set to `per-conversation` the process outlives each mention, so it caps a whole session's lifetime. Default: `0`.",
      "type": "integer",
      "minimum": 0,
      "maximum": 9007199254740991,
      "markdownDescription": "Kill the command after this many milliseconds. `0` means never, which is the right choice for a long-running agent. What it measures follows `lifecycle`: under the default `per-mention` it is one mention's run, and when `lifecycle` is set to `per-conversation` the process outlives each mention, so it caps a whole session's lifetime. Default: `0`."
    },
    "lifecycle": {
      "default": "per-mention",
      "description": "How the command is run.\n\n- `per-mention` starts the command fresh for every mention, hands it one pretty-printed JSON object on stdin, and posts its reply file once it exits.\n- `per-conversation` keeps one process alive per thread, writes each later mention to its stdin as one more line of JSON, and posts replies as they are written.\n\nDefault: `per-mention`.",
      "type": "string",
      "enum": [
        "per-mention",
        "per-conversation"
      ],
      "markdownDescription": "How the command is run.\n\n- `per-mention` starts the command fresh for every mention, hands it one pretty-printed JSON object on stdin, and posts its reply file once it exits.\n- `per-conversation` keeps one process alive per thread, writes each later mention to its stdin as one more line of JSON, and posts replies as they are written.\n\nDefault: `per-mention`."
    },
    "sessionIdleMs": {
      "default": 0,
      "description": "Used only when `lifecycle` is set to `per-conversation`: close a session after this many milliseconds with no new mention. `0` keeps it alive indefinitely, so one process stays live per conversation the forwarder has seen. Ignored under the default `per-mention` lifecycle, where the command exits on its own after every mention. Default: `0`.",
      "type": "integer",
      "minimum": 0,
      "maximum": 9007199254740991,
      "markdownDescription": "Used only when `lifecycle` is set to `per-conversation`: close a session after this many milliseconds with no new mention. `0` keeps it alive indefinitely, so one process stays live per conversation the forwarder has seen. Ignored under the default `per-mention` lifecycle, where the command exits on its own after every mention. Default: `0`."
    },
    "replyDebounceMs": {
      "default": 1500,
      "description": "Used only when `lifecycle` is set to `per-conversation`: how long writes to a reply file must settle, in milliseconds, before whatever was appended is posted. Ignored under the default `per-mention` lifecycle, where the reply file is posted once the command exits. Default: `1500`.",
      "type": "integer",
      "minimum": 0,
      "maximum": 9007199254740991,
      "markdownDescription": "Used only when `lifecycle` is set to `per-conversation`: how long writes to a reply file must settle, in milliseconds, before whatever was appended is posted. Ignored under the default `per-mention` lifecycle, where the reply file is posted once the command exits. Default: `1500`."
    },
    "replyDir": {
      "description": "Directory to keep reply files in. Set it if you want to read them yourself. Default: a fresh temp directory, printed at startup.",
      "type": "string",
      "markdownDescription": "Directory to keep reply files in. Set it if you want to read them yourself. Default: a fresh temp directory, printed at startup."
    },
    "includeRawPayload": {
      "default": false,
      "description": "Add the platform's untouched webhook payload as `raw` in the JSON written to the command's stdin.\n\nThis adds no placeholder of its own: there is no `{{raw}}`, and writing one is rejected at startup as unknown. What it does change is `{{json}}` and `MENTION_JSON`, which serialize the whole mention and so grow by the size of the payload. Read the payload from stdin rather than either of those: a payload may be as large as `maxPayloadBytes`, and the operating system caps how much can go in argv and the environment.\n\nDefault: `false`.",
      "type": "boolean",
      "markdownDescription": "Add the platform's untouched webhook payload as `raw` in the JSON written to the command's stdin.\n\nThis adds no placeholder of its own: there is no `{{raw}}`, and writing one is rejected at startup as unknown. What it does change is `{{json}}` and `MENTION_JSON`, which serialize the whole mention and so grow by the size of the payload. Read the payload from stdin rather than either of those: a payload may be as large as `maxPayloadBytes`, and the operating system caps how much can go in argv and the environment.\n\nDefault: `false`."
    },
    "logPayloads": {
      "default": false,
      "description": "Log every incoming delivery as pretty-printed JSON, including ones that matched no trigger phrase and event types the forwarder does not act on. Independent of `logLevel`, so turning it on is enough on its own. Default: `false`.",
      "type": "boolean",
      "markdownDescription": "Log every incoming delivery as pretty-printed JSON, including ones that matched no trigger phrase and event types the forwarder does not act on. Independent of `logLevel`, so turning it on is enough on its own. Default: `false`."
    },
    "logLevel": {
      "default": "info",
      "description": "How much to log.\n\n- `debug` why a delivery was skipped: a duplicate, a bot author, or a name absent from `allowedAuthors`.\n- `info` accepted mentions, the command starting and finishing, and replies posted.\n- `warn` refused webhooks, and best-effort failures such as a reaction or a reply that did not go through.\n- `error` outright failures only, such as a command exiting non-zero.\n\n`warn` and `error` are written to stderr, `debug` and `info` to stdout. Default: `info`.",
      "type": "string",
      "enum": [
        "debug",
        "info",
        "warn",
        "error"
      ],
      "markdownDescription": "How much to log.\n\n- `debug` why a delivery was skipped: a duplicate, a bot author, or a name absent from `allowedAuthors`.\n- `info` accepted mentions, the command starting and finishing, and replies posted.\n- `warn` refused webhooks, and best-effort failures such as a reaction or a reply that did not go through.\n- `error` outright failures only, such as a command exiting non-zero.\n\n`warn` and `error` are written to stderr, `debug` and `info` to stdout. Default: `info`."
    },
    "maxConcurrentConversations": {
      "default": 4,
      "description": "How many different conversations may have a command in flight at once. Mentions within one conversation always run in arrival order and never at the same time, whatever this is set to, and nothing is dropped when the limit is reached. `1` makes everything strictly sequential. Default: `4`.",
      "type": "integer",
      "minimum": 1,
      "maximum": 9007199254740991,
      "markdownDescription": "How many different conversations may have a command in flight at once. Mentions within one conversation always run in arrival order and never at the same time, whatever this is set to, and nothing is dropped when the limit is reached. `1` makes everything strictly sequential. Default: `4`."
    },
    "reactionEmoji": {
      "default": "eyes",
      "description": "Emoji name used to acknowledge a mention that was accepted and queued, with or without surrounding colons. GitHub takes only its own fixed set (`eyes`, `+1`, `-1`, `laugh`, `hooray`/`tada`, `confused`, `heart`, `rocket`); anything else still works on Slack and Linear and is skipped on GitHub, with a warning at startup. Default: `eyes`.",
      "type": "string",
      "minLength": 1,
      "markdownDescription": "Emoji name used to acknowledge a mention that was accepted and queued, with or without surrounding colons. GitHub takes only its own fixed set (`eyes`, `+1`, `-1`, `laugh`, `hooray`/`tada`, `confused`, `heart`, `rocket`); anything else still works on Slack and Linear and is skipped on GitHub, with a warning at startup. Default: `eyes`."
    },
    "ignoreBots": {
      "default": true,
      "description": "Skip mentions written by bots. Keep this on: it is what stops the bot from re-triggering itself on its own replies. Default: `true`.",
      "type": "boolean",
      "markdownDescription": "Skip mentions written by bots. Keep this on: it is what stops the bot from re-triggering itself on its own replies. Default: `true`."
    },
    "ignoreAuthors": {
      "default": [],
      "description": "Additional author names or logins to skip, compared case-insensitively.",
      "type": "array",
      "items": {
        "type": "string"
      },
      "markdownDescription": "Additional author names or logins to skip, compared case-insensitively."
    },
    "github": {
      "description": "GitHub settings. GitHub turns on when `GITHUB_WEBHOOK_SECRET` is in the environment rather than because this block is present, and `triggerPhrases` is required once it is on.",
      "type": "object",
      "properties": {
        "path": {
          "description": "URL path GitHub posts webhooks to. Must start with `/`. Default: `/github/webhooks`.",
          "type": "string",
          "pattern": "^\\/.*",
          "markdownDescription": "URL path GitHub posts webhooks to. Must start with `/`. Default: `/github/webhooks`."
        },
        "triggerPhrases": {
          "description": "Phrases that count as a mention, such as `[\"@my-bot\"]`. Matched case-insensitively and only as whole tokens, so `@my-bot` fires on `hey @my-bot, look` but not on `@my-botswana`, `@my-bot-2`, or `me@my-bot`. Required while GitHub is enabled: a GitHub webhook never reports that a mention happened, so without a phrase every comment would fire the command.",
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          },
          "markdownDescription": "Phrases that count as a mention, such as `[\"@my-bot\"]`. Matched case-insensitively and only as whole tokens, so `@my-bot` fires on `hey @my-bot, look` but not on `@my-botswana`, `@my-bot-2`, or `me@my-bot`. Required while GitHub is enabled: a GitHub webhook never reports that a mention happened, so without a phrase every comment would fire the command."
        },
        "allowedAuthors": {
          "description": "Only these authors may trigger the command on this platform. Absent or empty means anyone may, which on a public repo means anyone who can leave a comment. Compared case-insensitively against one string per platform: a GitHub login, a Slack display name, or a Linear user name. Run with `\"logLevel\": \"debug\"` and read the `author=` field to see the exact string it is compared against.",
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          },
          "markdownDescription": "Only these authors may trigger the command on this platform. Absent or empty means anyone may, which on a public repo means anyone who can leave a comment. Compared case-insensitively against one string per platform: a GitHub login, a Slack display name, or a Linear user name. Run with `\"logLevel\": \"debug\"` and read the `author=` field to see the exact string it is compared against."
        },
        "allowedSources": {
          "description": "Addresses or CIDR ranges GitHub's webhooks may arrive from. Replaces the built-in list rather than adding to it, and an empty array turns the source check off entirely. Loopback always passes either way, which is what a tunnelled delivery arrives on. Setting it also pins the list: the startup fetch and the daily refresh both stop, so a rotated GitHub range starts coming back `403` until you edit this. Default: the hook ranges read from `api.github.com/meta` at startup and once a day after.",
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          },
          "markdownDescription": "Addresses or CIDR ranges GitHub's webhooks may arrive from. Replaces the built-in list rather than adding to it, and an empty array turns the source check off entirely. Loopback always passes either way, which is what a tunnelled delivery arrives on. Setting it also pins the list: the startup fetch and the daily refresh both stop, so a rotated GitHub range starts coming back `403` until you edit this. Default: the hook ranges read from `api.github.com/meta` at startup and once a day after."
        },
        "apiUrl": {
          "description": "Override the GitHub API base URL, for GitHub Enterprise Server or a local stub. Any value other than `https://api.github.com` also lifts Octokit's write pacing, which is there for github.com's own rate limits. Default: GitHub's own.",
          "type": "string",
          "format": "uri",
          "markdownDescription": "Override the GitHub API base URL, for GitHub Enterprise Server or a local stub. Any value other than `https://api.github.com` also lifts Octokit's write pacing, which is there for github.com's own rate limits. Default: GitHub's own."
        }
      },
      "additionalProperties": false,
      "markdownDescription": "GitHub settings. GitHub turns on when `GITHUB_WEBHOOK_SECRET` is in the environment rather than because this block is present, and `triggerPhrases` is required once it is on."
    },
    "slack": {
      "description": "Slack settings. Slack turns on when `SLACK_SIGNING_SECRET` and `SLACK_BOT_TOKEN` are in the environment rather than because this block is present.",
      "type": "object",
      "properties": {
        "path": {
          "description": "URL path Slack posts events to. Must start with `/`. Default: `/slack/events`.",
          "type": "string",
          "pattern": "^\\/.*",
          "markdownDescription": "URL path Slack posts events to. Must start with `/`. Default: `/slack/events`."
        },
        "triggerPhrases": {
          "description": "An optional second way in rather than a filter: a native `app_mention` always counts, and so does any message carrying one of these phrases, such as `[\"@my-bot\"]`. Matched case-insensitively and only as whole tokens, so `@my-bot` fires on `hey @my-bot, look` but not on `@my-botswana`, `@my-bot-2`, or `me@my-bot`. Reaching a plain message needs extra event subscriptions; a DM needs neither a mention nor a phrase. Default: none, so only a real mention counts.",
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          },
          "markdownDescription": "An optional second way in rather than a filter: a native `app_mention` always counts, and so does any message carrying one of these phrases, such as `[\"@my-bot\"]`. Matched case-insensitively and only as whole tokens, so `@my-bot` fires on `hey @my-bot, look` but not on `@my-botswana`, `@my-bot-2`, or `me@my-bot`. Reaching a plain message needs extra event subscriptions; a DM needs neither a mention nor a phrase. Default: none, so only a real mention counts."
        },
        "allowedAuthors": {
          "description": "Only these authors may trigger the command on this platform. Absent or empty means anyone may, which on a public repo means anyone who can leave a comment. Compared case-insensitively against one string per platform: a GitHub login, a Slack display name, or a Linear user name. Run with `\"logLevel\": \"debug\"` and read the `author=` field to see the exact string it is compared against.",
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          },
          "markdownDescription": "Only these authors may trigger the command on this platform. Absent or empty means anyone may, which on a public repo means anyone who can leave a comment. Compared case-insensitively against one string per platform: a GitHub login, a Slack display name, or a Linear user name. Run with `\"logLevel\": \"debug\"` and read the `author=` field to see the exact string it is compared against."
        },
        "allowedSources": {
          "description": "Addresses or CIDR ranges Slack's events may arrive from. Replaces the built-in list rather than adding to it, and an empty array turns the source check off entirely. Loopback always passes either way, which is what a tunnelled delivery arrives on. Default: no source check at all, because Slack runs on AWS and publishes no ranges of its own.",
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          },
          "markdownDescription": "Addresses or CIDR ranges Slack's events may arrive from. Replaces the built-in list rather than adding to it, and an empty array turns the source check off entirely. Loopback always passes either way, which is what a tunnelled delivery arrives on. Default: no source check at all, because Slack runs on AWS and publishes no ranges of its own."
        },
        "apiUrl": {
          "description": "Override the Slack API base URL, for Enterprise Grid or a local stub. Default: Slack's own.",
          "type": "string",
          "format": "uri",
          "markdownDescription": "Override the Slack API base URL, for Enterprise Grid or a local stub. Default: Slack's own."
        }
      },
      "additionalProperties": false,
      "markdownDescription": "Slack settings. Slack turns on when `SLACK_SIGNING_SECRET` and `SLACK_BOT_TOKEN` are in the environment rather than because this block is present."
    },
    "linear": {
      "description": "Linear settings. Linear turns on when `LINEAR_WEBHOOK_SECRET` is in the environment rather than because this block is present, and `triggerPhrases` is required once it is on.",
      "type": "object",
      "properties": {
        "path": {
          "description": "URL path Linear posts webhooks to. Must start with `/`. Default: `/linear/webhooks`.",
          "type": "string",
          "pattern": "^\\/.*",
          "markdownDescription": "URL path Linear posts webhooks to. Must start with `/`. Default: `/linear/webhooks`."
        },
        "triggerPhrases": {
          "description": "Phrases that count as a mention, such as `[\"@my-bot\"]`. Matched case-insensitively and only as whole tokens, so `@my-bot` fires on `hey @my-bot, look` but not on `@my-botswana`, `@my-bot-2`, or `me@my-bot`. Required while Linear is enabled: a Linear webhook never reports that a mention happened, so without a phrase every comment would fire the command.",
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          },
          "markdownDescription": "Phrases that count as a mention, such as `[\"@my-bot\"]`. Matched case-insensitively and only as whole tokens, so `@my-bot` fires on `hey @my-bot, look` but not on `@my-botswana`, `@my-bot-2`, or `me@my-bot`. Required while Linear is enabled: a Linear webhook never reports that a mention happened, so without a phrase every comment would fire the command."
        },
        "allowedAuthors": {
          "description": "Only these authors may trigger the command on this platform. Absent or empty means anyone may, which on a public repo means anyone who can leave a comment. Compared case-insensitively against one string per platform: a GitHub login, a Slack display name, or a Linear user name. Run with `\"logLevel\": \"debug\"` and read the `author=` field to see the exact string it is compared against.",
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          },
          "markdownDescription": "Only these authors may trigger the command on this platform. Absent or empty means anyone may, which on a public repo means anyone who can leave a comment. Compared case-insensitively against one string per platform: a GitHub login, a Slack display name, or a Linear user name. Run with `\"logLevel\": \"debug\"` and read the `author=` field to see the exact string it is compared against."
        },
        "allowedSources": {
          "description": "Addresses or CIDR ranges Linear's webhooks may arrive from. Replaces the built-in list rather than adding to it, and an empty array turns the source check off entirely. Loopback always passes either way, which is what a tunnelled delivery arrives on. Default: the addresses Linear publishes, bundled in `src/request-guard.ts` because Linear offers no endpoint to read them from.",
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          },
          "markdownDescription": "Addresses or CIDR ranges Linear's webhooks may arrive from. Replaces the built-in list rather than adding to it, and an empty array turns the source check off entirely. Loopback always passes either way, which is what a tunnelled delivery arrives on. Default: the addresses Linear publishes, bundled in `src/request-guard.ts` because Linear offers no endpoint to read them from."
        },
        "apiUrl": {
          "description": "Override the Linear GraphQL endpoint, for a local stub. Default: Linear's own.",
          "type": "string",
          "format": "uri",
          "markdownDescription": "Override the Linear GraphQL endpoint, for a local stub. Default: Linear's own."
        }
      },
      "additionalProperties": false,
      "markdownDescription": "Linear settings. Linear turns on when `LINEAR_WEBHOOK_SECRET` is in the environment rather than because this block is present, and `triggerPhrases` is required once it is on."
    }
  },
  "required": [
    "command"
  ],
  "additionalProperties": false,
  "title": "mention-forwarder config",
  "description": "Configuration for mention-forwarder, which forwards @-mentions from GitHub, Slack, and Linear into a command. Only `command` is required, and each platform turns on through its secret in the environment rather than through this file. README.md is the full reference.",
  "markdownDescription": "Configuration for mention-forwarder, which forwards @-mentions from GitHub, Slack, and Linear into a command. Only `command` is required, and each platform turns on through its secret in the environment rather than through this file. README.md is the full reference."
}
